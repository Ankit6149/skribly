name: Storage integration apply

on:
  pull_request:
    paths:
      - scripts/storage/apply-issue-14-integration.mjs
      - .github/workflows/storage-integration-apply.yml

permissions:
  contents: write

jobs:
  apply-storage-integration:
    name: Apply checked storage integration
    if: >-
      github.head_ref == 'fix/14-crash-safe-storage' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Check out exact pull-request branch
        uses: actions/checkout@v4
        with:
          ref: fix/14-crash-safe-storage
          fetch-depth: 1

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Apply checked integration transform
        run: node scripts/storage/apply-issue-14-integration.mjs

      - name: Format Rust source
        run: cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml

      - name: Commit transformed product files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            apps/desktop/src-tauri/src/lib.rs \
            apps/desktop/src/stores/skribStore.ts \
            apps/desktop/src/features/skribs/SkribComposer.tsx \
            apps/desktop/src/styles/note-experience.css
          if git diff --cached --quiet; then
            echo "No integration changes were produced"
            exit 1
          fi
          git commit -m "feat(storage): integrate recovery service with app state"
          git push origin HEAD:fix/14-crash-safe-storage
